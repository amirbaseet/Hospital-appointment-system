@model IEnumerable<PatientUser>

@{
    ViewData["Title"] = "Patient Dashboard";
}

<div class="container p-3 ">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">  @ViewData["AdminsList"] </h2>
        </div>
        <div class="col-6 text-right">
            <a asp-controller="PatientUser" asp-action="CreateAdmin" class="btn btn-primary">
                <i class="bi bi-person-add"></i> &nbsp; @ViewData["AddanAdmin"]
            </a>
            </div>
    </div>

</div>
<br />
<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <td>@Html.DisplayNameFor(model=>model.Id)</td>
            <td>@Html.DisplayNameFor(Model=>Model.UserName)</td>
            <th>@Html.DisplayNameFor(Model=>Model.Gender)</th>
            <td>@Html.DisplayNameFor(Model=>Model.Email)</td>
            <!-- Display other properties as needed -->
            <th></th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.Id
                </td>
                <td width="20%">
                    @item.UserName
                </td>
                <td>
                    @item.Gender
                </td>
                <td>
                    @item.Email
                </td>
                <td>
                    <div class="btn-group" role="group">
                        <a asp-controller="PatientUser" asp-action="EditAdmin" asp-route-id="@item.Id"
                           class="btn btn-warning"> <i class="bi bi-pencil-square"></i>@ViewData["Edit"]</a>
                        <a asp-controller="PatientUser" asp-action="DeleteAdmin" asp-route-id="@item.Id"
                           class="btn btn-danger"> <i class="bi bi-trash"></i>@ViewData["Delete"]</a>

                    </div>
                </td>
            </tr>
        }
    </tbody>

</table>
</div>

